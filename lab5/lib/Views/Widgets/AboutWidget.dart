import 'package:flutter/material.dart';

class Aboutwidget extends StatelessWidget {
  const Aboutwidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("About Author"));
  }
}
